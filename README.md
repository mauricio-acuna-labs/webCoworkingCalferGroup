# Calfer Workspace — web pública

Landing comercial pública de Calfer Workspace, independiente del backoffice operativo.

## Vista local

```powershell
python -m http.server 4173
```

Abrir `http://localhost:4173`. Cada cambio enviado a `main` se publica automáticamente mediante GitHub Pages.
